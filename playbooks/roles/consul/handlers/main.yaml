- name: Restart Consul
  ansible.builtin.systemd:
    name: consul
    state: restarted
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
- name: Reload Consul
  ansible.builtin.shell:
    cmd: consul reload
